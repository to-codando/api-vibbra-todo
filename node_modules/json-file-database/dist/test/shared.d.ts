import type { DatabaseOptions } from '../src';
export declare function sleep(delay: number): Promise<void>;
export interface Obj {
    id: number;
    name: string;
}
export declare const OBJS_ARRAY: {
    id: number;
    name: string;
}[];
export declare function connectDatabase(options?: Partial<DatabaseOptions>): import("../src").Database;
